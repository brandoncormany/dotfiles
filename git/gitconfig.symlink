[user]
	name = Brandon Cormany
	email = brandoncormany@gmail.com
[credential]
	helper = osxkeychain
[diff]
	tool = atom
[core]
	editor = atom --wait
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
